using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Compiler.Core
{
    internal static class Utility
    {
//        internal static T Find<T>(string name, BaseNodeName<T> collection, bool ignoreCase) where T : BaseNodeName<T>
//        {
//#if MyCode
//            return collection.Where(id => string.Compare(id.Name, name, ignoreCase) == 0).FirstOrDefault();
//#endif
//            T temp = (T)collection;
//            while (temp != null)
//            {
//                if (string.Compare(temp.Name, name, ignoreCase) == 0)
//                {
//                    return temp;
//                }
//                temp = (T)temp.Next;
//            }

//            return null;
//        }
    }
}
